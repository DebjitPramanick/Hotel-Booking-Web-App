import styled from "styled-components"

export const SidebarContainer = styled.div`
    background: grey;
    border-radius: 10px;
    position: fixed;
    width: 460px;
    height: calc(100vh - 100px);
    overflow-y: scroll;
    padding: 20px;
`